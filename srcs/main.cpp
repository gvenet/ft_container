#include <iostream>

#include "../inc/map.hpp"
#include "../inc/utils/pair.hpp"
#include <map>

int main() {

	// ft::pair<const int, int>* addr;
	ft::map<int, int> m;

	// m[1] = 10;
	
	// addr = &*m.begin();

	// std::cout << (*addr).first << "\n";
}